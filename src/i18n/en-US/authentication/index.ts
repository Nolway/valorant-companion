import type { BaseTranslation } from '../../i18n-types'

const en_US_authentication = {
    logout: {
        reply: 'You have been logged out.',
    },
} satisfies BaseTranslation;

export default en_US_authentication;
