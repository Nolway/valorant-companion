import type { NamespaceAuthenticationTranslation } from '../../i18n-types'

const fr_authentication = {
  logout: {
    reply: 'You have been logged out.',
  },
} satisfies NamespaceAuthenticationTranslation;

export default fr_authentication;
